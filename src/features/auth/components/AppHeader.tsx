import HighlightTitle from "@/components/HighlightTitle";

const AppHeader = () => {
  return (
    <div className="flex items-center justify-center h-20 shadow-sm bg-card">
      <HighlightTitle> WolfStream</HighlightTitle>
    </div>
  );
};

export default AppHeader;
